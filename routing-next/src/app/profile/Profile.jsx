// Componente (esto es estatico)
// no hay problema en que tenga el mismo nombre de la ruta DINAMICA
// Porque se priorizan las rutas ESTÁTICAS (definidas)
export const Profile = () => {
    return (
        <div>
            <h1>Profile</h1>
        </div>
    );
}

export default Profile;